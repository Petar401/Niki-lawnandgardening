/**
 * Niki's contact details. Edit here to wire the contact form to a real
 * inbox / number / area name without hunting through components.
 *
 * The form submits via `mailto:` (no backend, no API key, no CORS), so
 * any address you put here will receive submissions through the
 * visitor's email client.
 */
export const CONTACT_EMAIL = 'hello@niki-lawn.example';
export const CONTACT_PHONE = '';                       // optional, e.g. '+1 (555) 555 0123'
export const CONTACT_AREA = 'Across the neighbourhood';
